# Backend
Flask
flask-cors
openai
python-dotenv

# Database (Phase 1)
psycopg2-binary

# Auth (Phase 2)
passlib[bcrypt]
bcrypt==4.0.1
PyJWT
authlib
requests

# Real-time (Phase 5)
flask-socketio

# Encryption (Phase 6)
cryptography
